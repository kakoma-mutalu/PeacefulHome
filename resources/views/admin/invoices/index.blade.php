@extends('layouts.admin')
@section('admin')
<div class="row g-4"><div class="col-lg-8"><h1>Billing</h1><div class="card p-3 mt-3"><div class="table-responsive"><table class="table"><thead><tr><th>Invoice</th><th>Patient</th><th>Amount</th><th>Status</th><th>Payment</th></tr></thead><tbody>@foreach($invoices as $i)<tr><td>{{ $i->invoice_number }}<br><small>{{ $i->description }}</small></td><td>{{ $i->patient->first_name }} {{ $i->patient->last_name }}</td><td>ZMW {{ number_format($i->amount,2) }}</td><td>{{ ucfirst($i->status) }}</td><td><form method="POST" action="{{ route('admin.invoices.payments',$i) }}" class="d-flex gap-1">@csrf<input name="amount" type="number" step="0.01" class="form-control form-control-sm" placeholder="Amount" required><select name="method" class="form-select form-select-sm"><option value="cash">Cash</option><option value="bank">Bank</option><option value="mobile_money">Mobile Money</option><option value="card">Card</option></select><input name="reference" class="form-control form-control-sm" placeholder="Ref"><button class="btn btn-sm btn-green">Record</button></form></td></tr>@endforeach</tbody></table></div>{{ $invoices->links() }}</div></div>
<div class="col-lg-4"><div class="card p-4"><h5>Create invoice</h5><form method="POST" action="{{ route('admin.invoices.store') }}">@csrf<div class="mb-2"><label>Patient</label><select name="patient_id" class="form-select">@foreach($patients as $p)<option value="{{ $p->id }}">{{ $p->first_name }} {{ $p->last_name }}</option>@endforeach</select></div><div class="mb-2"><label>Description</label><input name="description" class="form-control" required></div><div class="mb-2"><label>Amount</label><input name="amount" type="number" step="0.01" class="form-control" required></div><div class="mb-2"><label>Due date</label><input name="due_date" type="date" class="form-control" required></div><button class="btn btn-green w-100">Create invoice</button></form></div></div></div>
@endsection
