@extends('layouts.admin')
@section('admin')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h1>{{ $patient->first_name }} {{ $patient->last_name }}</h1><p class="text-muted">{{ $patient->patient_number }}</p></div><span class="badge badge-soft">{{ ucfirst($patient->status) }}</span></div>
<div class="row g-4"><div class="col-lg-7"><div class="card p-4"><h5>Patient overview</h5><div class="row g-3 mt-1"><div class="col-md-6"><small class="text-muted">Phone</small><div>{{ $patient->phone }}</div></div><div class="col-md-6"><small class="text-muted">Email</small><div>{{ $patient->email ?: '—' }}</div></div><div class="col-md-6"><small class="text-muted">Date of birth</small><div>{{ $patient->date_of_birth?->format('d M Y') ?: '—' }}</div></div><div class="col-md-6"><small class="text-muted">Gender</small><div>{{ $patient->gender ?: '—' }}</div></div></div></div></div><div class="col-lg-5"><div class="card p-4"><h5>Emergency contact</h5><p class="mb-1">{{ $patient->emergency_contact_name ?: '—' }}</p><p class="text-muted">{{ $patient->emergency_contact_phone ?: '—' }}</p><hr><h6>Notes</h6><p class="text-muted">{{ $patient->notes ?: 'No notes recorded.' }}</p></div></div></div>
@endsection
