@extends('layouts.admin')
@section('admin')
<div class="d-flex justify-content-between align-items-center mb-3"><div><h1>Patients</h1><p class="text-muted">Central patient registry.</p></div><a class="btn btn-green" href="{{ route('admin.patients.create') }}"><i class="bi bi-plus"></i> Register patient</a></div>
<div class="card p-3"><form class="row g-2 mb-3"><div class="col-md-8"><input name="q" value="{{ $q }}" class="form-control" placeholder="Search name or patient number"></div><div class="col-md-2"><button class="btn btn-outline-secondary w-100">Search</button></div></form><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Number</th><th>Patient</th><th>Phone</th><th>Status</th><th></th></tr></thead><tbody>@forelse($patients as $p)<tr><td>{{ $p->patient_number }}</td><td><strong>{{ $p->first_name }} {{ $p->last_name }}</strong><br><small class="text-muted">{{ $p->email }}</small></td><td>{{ $p->phone }}</td><td><span class="badge badge-soft">{{ ucfirst($p->status) }}</span></td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.patients.show',$p) }}">Open</a></td></tr>@empty<tr><td colspan="5">No patients found.</td></tr>@endforelse</tbody></table></div>{{ $patients->links() }}</div>
@endsection
