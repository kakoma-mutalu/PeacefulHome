@extends('layouts.public') @section('content')<x-public-page-title eyebrow="Reservations" title="Start your recovery enquiry." text="Choose a programme or service and tell us your preferred date. Submission is not final admission confirmation."/><section class="section"><div class="container"><div class="row justify-content-center"><div class="col-lg-9"><div class="content-card"><form method="POST" action="{{ route('booking.store') }}">@csrf<div class="row g-3"><div class="col-md-6"><label class="form-label" for="service_id">Programme or service</label><select id="service_id" name="service_id" class="form-select" required>@foreach($services as $s)<option value="{{ $s->id }}" @selected(request('service') == $s->id)>{{ $s->name }} — @if($s->price > 0) ZMW {{ number_format($s->price,2) }} @else Confirm fees @endif</option>@endforeach</select></div><div class="col-md-6"><label class="form-label" for="customer_name">Full name</label><input id="customer_name" name="customer_name" class="form-control" required value="{{ old('customer_name') }}"></div><div class="col-md-6"><label class="form-label" for="customer_email">Email</label><input id="customer_email" name="customer_email" type="email" class="form-control" required value="{{ old('customer_email') }}"></div><div class="col-md-6"><label class="form-label" for="customer_phone">Phone</label><input id="customer_phone" name="customer_phone" class="form-control" required value="{{ old('customer_phone') }}"></div><div class="col-md-6"><label class="form-label" for="booking_date">Preferred date</label><input id="booking_date" name="booking_date" type="date" min="{{ date('Y-m-d') }}" class="form-control" required value="{{ old('booking_date') }}"></div><div class="col-md-6"><label class="form-label" for="booking_time">Preferred time</label><input id="booking_time" name="booking_time" type="time" class="form-control" required value="{{ old('booking_time') }}"></div><div class="col-12"><label class="form-label" for="notes">Anything you would like us to know? <span class="text-muted">(optional)</span></label><textarea id="notes" name="notes" class="form-control" rows="4">{{ old('notes') }}</textarea></div><div class="col-12"><button class="btn btn-green btn-lg">Submit reservation</button></div></div></form></div></div></div></div></section>@endsection
