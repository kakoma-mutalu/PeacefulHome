@extends('layouts.public') @section('content')<x-public-page-title eyebrow="Our services" title="Support that meets people where they are." text="Every service is individualised, subject to assessment and designed to support recovery without promises that cannot be guaranteed."/><section class="section"><div class="container"><div class="row g-4">@foreach($services as $service)<div class="col-md-6"><article class="service-card detail-card h-100"><span>{{ $service->category }}</span><h2>{{ $service->name }}</h2><p>{{ $service->description }}</p><p><strong>Who it is for:</strong> {{ $service->audience }}</p><a class="btn btn-outline-navy" href="{{ route('services.show',$service) }}">Service details</a></article></div>@endforeach</div></div></section>@endsection
